/target

# Test data directory (local testing files, not for repo)
test-data/

# Internal documentation (not for public repo)
docs/

# Examples that might contain sensitive data
examples/*.spdx.json
examples/*.cdx.json
examples/*.vex.json

# Keep only schema files in schemas directory
!schemas/*.json
